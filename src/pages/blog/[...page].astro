---
import BaseLayout from '../../layouts/BaseLayout.astro';
import BlogPreview from '../../components/BlogPreview.astro';
import Pagination from '../../components/Pagination.astro';
import { getCollection } from 'astro:content';
import { filterPublishedPosts, sortPostsByDate } from '../../utils/posts';

export async function getStaticPaths({ paginate }) {
  const posts = await getCollection('blog');
  const publishedPosts = filterPublishedPosts(posts);
  const sortedPosts = sortPostsByDate(publishedPosts);

  return paginate(sortedPosts, { pageSize: 10 });
}

const { page } = Astro.props;
---

<BaseLayout title="M-Zen | Blog">
  <h1 class="text-4xl font-bold mb-8  text-gray-900 dark:text-white">Blog | 累積想法</h1>
  <p class="text-xl mb-8 text-gray-600 dark:text-gray-300">不要讓「AI比你更懂你」，持續累積自身的想法，漸漸浮現更清晰的自我。</p>
  <section>
    {page.data.map((post) => <BlogPreview post={post} />)}
  </section>
  <Pagination
    currentPage={page.currentPage}
    totalPages={page.lastPage}
    baseUrl="/blog/"
  />
</BaseLayout>